# 搜打撤游戏 - 7天实施计划

## Context

在 UE5 中从零构建一个单机"搜刮-战斗-撤离"游戏。目标是 7 天内完成可玩的核心循环：玩家在小地图中出生 → 搜刮武器/物资 → 与 AI 敌人交战 → 到达撤离点获胜。

**开发方式:** 蓝图为主，少量 C++（仅在蓝图无法优雅实现的场景写 C++，如 Struct/Enum 定义、性能敏感的检测逻辑）  
**开发者水平:** 初学者  
**素材方案:** Epic 免费商城资源 + Mixamo 动画  
**UE 版本:** 5.4+（推荐 5.5）

---

## 总体架构

```
Contents/
├── Core/
│   ├── Enums/E_WeaponType        — 武器类型枚举
│   ├── Structs/S_InventoryItem   — 背包物品结构体
│   └── GameMode/GM_Extraction    — 撤离游戏模式
├── Character/
│   ├── BP_PlayerCharacter        — 玩家角色 (Character)
│   ├── BP_EnemyCharacter         — 敌人角色 (Character)
│   └── AC_HealthComponent        — 血量组件 (ActorComponent)
├── Items/
│   ├── BP_PickupBase             — 拾取物基类
│   ├── BP_WeaponPickup           — 武器拾取物
│   ├── BP_AmmoPickup             — 弹药拾取物
│   ├── BP_HealthPickup           — 医疗拾取物
│   ├── BP_SearchableContainer    — 可搜索宝箱
│   └── DT_ContainerLoot          — 宝箱掉落数据表
├── Weapons/
│   ├── BP_WeaponBase             — 武器基类
│   ├── BP_Weapon_Rifle           — 步枪
│   └── BP_Weapon_Pistol          — 手枪
├── AI/
│   ├── BT_Enemy                  — 行为树
│   ├── BB_Enemy                  — 黑板
│   └── BT_Task_*                 — 自定义任务节点
├── UI/
│   ├── WBP_HUD                   — 主 HUD
│   ├── WBP_Inventory             — 背包面板
│   └── WBP_GameResult            — 胜负结果
└── Maps/
    └── L_ExtractionTest          — 测试关卡
```

---

## Day 1: 项目创建 + 角色移动 + 基础相机

**目标：** 能在一个地形上走路、跑步、跳跃、转动视角

### 1.1 创建项目 (10分钟)

1. 打开 Epic Games Launcher → 启动 UE5.4/5.5
2. 选择 **Games** → **Third Person** 模板
3. 项目设置：
   - Blueprint: ✓（不要 C++ 模板，之后单独加 C++ 类）
   - 包含初学者内容包: ✓
   - 光线追踪: 关闭（初学者性能友好）
   - 项目名: `ExtractionGame`
4. 创建后先别动，等 Shader 编译完成（右下角进度条消失）

### 1.2 熟悉已有内容 (15分钟)

1. 模板自带 `BP_ThirdPersonCharacter` — 这就是你的玩家角色，已含移动/跳跃/相机
2. 按 **Alt+P** 或点工具栏 Play 按钮进入 PIE（Play In Editor）
3. 确认 WASD 移动、空格跳跃、鼠标转视角正常
4. 退出 PIE

### 1.3 创建基础关卡 (20分钟)

1. Content Browser → Content 文件夹右键 → New Folder → 命名 `Maps`
2. 在 `Maps` 右键 → Level → 命名 `L_Extraction`
3. 双击 `L_Extraction` 打开空关卡
4. 从 Place Actors 面板拖入：
   - **Directional Light** — 主光源
   - **Sky Light** — 环境光
   - **Sky Atmosphere** — 天空大气（搜索栏输入名拖入）
   - **Volumetric Cloud** — 云
   - **Player Start** — 放在(0,0,0)，玩家出生点
5. 选中 Directional Light → 细节面板 → Transform → Rotation → 设为 `(-60, -45, 0)` 让光有角度
6. **Ctrl+S** 保存

### 1.4 添加简易地形 (15分钟)

1. 顶部工具栏 → 模式选择 → **Landscape**（山形图标）
2. 默认参数就行 → 点 **Create** 按钮
3. 创建后在 Landscape 模式下选 **Sculpt** 工具 → 刷一个略有起伏的地面
4. 不用太细，有地面能走就行
5. 回到 Selection 模式（箭头图标）

### 1.5 配置 GameMode 覆盖 (5分钟)

1. 打开 `L_Extraction` 关卡
2. 菜单栏 → Settings → **World Settings**
3. World Settings 面板 → GameMode Override → 选 `BP_ThirdPersonGameMode`（模板自带）
4. **Ctrl+S** 保存

### 1.6 验证 (5分钟)

1. 点击 Play → 角色能在地形上走动 → Day 1 完成

---

## Day 2: 拾取系统 + 背包

**目标：** 场景中的物品可以按 E 拾取，打开背包能看到

### 2.1 创建数据结构 (C++ 方式，更稳定)

> 注意：如果不想碰 C++，这一步可以完全用 Blueprint Struct 代替。路径：Content Browser 右键 → Blueprints → Structure。  
> 但 C++ 的 Enum/Struct 在后续蓝图节点中引用更稳定，推荐花 15 分钟做这个。

1. 编辑器菜单 → Tools → **New C++ Class**
2. 父类选择 **None**（直接创建头文件）→ 实际选 `UObject` 就行
3. 命名为 `ExtractionTypes` → 创建
4. Visual Studio 自动打开，替换文件内容为：

```cpp
// ExtractionTypes.h
#pragma once
#include "CoreMinimal.h"
#include "ExtractionTypes.generated.h"

UENUM(BlueprintType)
enum class EWeaponType : uint8
{
    Pistol      UMETA(DisplayName = "手枪"),
    Rifle       UMETA(DisplayName = "步枪"),
    Shotgun     UMETA(DisplayName = "霰弹枪")
};

UENUM(BlueprintType)
enum class EItemCategory : uint8
{
    Weapon      UMETA(DisplayName = "武器"),
    Ammo        UMETA(DisplayName = "弹药"),
    Health      UMETA(DisplayName = "医疗"),
    Misc        UMETA(DisplayName = "杂项")
};

USTRUCT(BlueprintType)
struct FInventorySlot
{
    GENERATED_BODY()

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    FName ItemID;

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    FText ItemName;

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    EItemCategory Category;

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    int32 Quantity = 0;

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    UTexture2D* Icon = nullptr;
};

USTRUCT(BlueprintType)
struct FWeaponData : public FTableRowBase
{
    GENERATED_BODY()

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    FText WeaponName;

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    EWeaponType WeaponType;

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    float BaseDamage = 10.0f;

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    float FireRate = 0.1f;  // 秒/发

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    int32 MagazineSize = 30;

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    float Range = 10000.0f;
};
```

5. 编译 (Ctrl+Alt+F11 或点击工具栏 Compile 按钮)
6. 关闭 VS，回到编辑器，等待 Hot Reload 完成

### 2.2 创建拾取物基类 (蓝图) (30分钟)

1. Content Browser → Content 右键 → Blueprint Class → 父类 **Actor** → 命名 `BP_PickupBase`
2. 双击打开蓝图编辑器
3. 添加组件（左上角 Add Component）：
   - **Static Mesh**（命名 `PickupMesh`）— 设为 Root
   - **Sphere Collision**（命名 `InteractionTrigger`）
     - 半径设为 150
     - Collision Presets → `OverlapAllDynamic`
     - 在 Details → Events → 勾选 `On Component Begin Overlap` 和 `On Component End Overlap`
4. 添加变量（左侧 My Blueprint → Variables → +）：
   - `ItemID` : Name
   - `ItemName` : Text  
   - `Category` : EItemCategory
   - `Quantity` : Integer (默认 1)
   - 每个变量勾选 `Instance Editable` + `Expose on Spawn`
5. 编译保存

**蓝图逻辑（Event Graph）：**

```
On Component Begin Overlap (InteractionTrigger)
  → Cast To BP_PlayerCharacter (Other Actor)
    → Branch
      [True] → Set "CanInteract" on Player = true
               → Set "CurrentInteractTarget" on Player = self
      [False] → (nothing)

On Component End Overlap (InteractionTrigger)
  → Cast To BP_PlayerCharacter
    → Branch
      [True] → Set "CanInteract" on Player = false
               → Set "CurrentInteractTarget" on Player = None
```

### 2.3 给玩家角色加交互系统 (30分钟)

1. 打开 `BP_ThirdPersonCharacter`（模板自带）
2. 添加变量：
   - `CanInteract` : Boolean (默认 false)
   - `CurrentInteractTarget` : BP_PickupBase (引用类型)
   - `Inventory` : FInventorySlot (Array)
   - `MaxInventorySlots` : Integer (默认 12)
3. 在 Event Graph 中添加键盘输入：

**设置输入映射：**
1. 菜单栏 → Edit → **Project Settings** → **Input**
2. 在 Action Mappings 点 +：
   - Action Name: `Interact` → Key: **E**
3. 关闭

**蓝图逻辑：**
```
InputAction Interact (事件，用 Enhanced Input 或 Legacy，模板默认用 Enhanced Input)
  → 在 BP_ThirdPersonCharacter 的 Setup Player Input 里已经绑定了 IA_* 类型的输入
  → 你需要添加 Interact 的 Input Action:
    1. Content Browser 右键 → Input → Input Action → 命名 IA_Interact
    2. 双击 IA_Interact → Value Type 选 Digital (bool)
    3. 回到 BP_ThirdPersonCharacter → Event Graph
    4. 找到 EnhancedInputAction 绑定区域，添加:
       EnhancedInputAction (IA_Interact) → Started → [交互逻辑]

交互逻辑:
  Branch (CanInteract)
    [True]:
      → Get CurrentInteractTarget
      → Break 其变量 (ItemID, ItemName, Category, Quantity)
      → 构造 FInventorySlot
      → 加入 Inventory 数组
      → DestroyActor (CurrentInteractTarget)
      → Set CanInteract = false
```

> **简化方案：** 如果 Enhanced Input 太复杂，可以先在 BP_ThirdPersonCharacter 的 Event Graph 里搜索 `InputAction` 找模板已有的示例（如 Jump），模仿其绑定方式。

### 2.4 放几个拾取物到关卡 (10分钟)

1. 从 Content Browser 拖 `BP_PickupBase` 到关卡中 → 放 3-4 个
2. 每个设置不同的 ItemName（如 "弹药箱"、"医疗包"、"M4A1"）
3. 给 PickupMesh 选一个 Static Mesh（初学者内容包里挑 Shape_Sphere / Shape_Cube）
4. **Ctrl+S** 保存关卡

### 2.5 验证

1. Play → 走到物品旁看到交互提示（可以先不加 HUD，用 Print String 打印 `CanInteract`）
2. 按 E → 物品消失，Inventory 数组数量 +1
3. 用 Blueprint Debugger 或 Print String 查看 Inventory 内容

---

### Day 2 附加：可搜索宝箱系统（塔科夫风格）

**目标：** 地图上放置宝箱/行李袋/武器箱，按住 F 搜索读条，打开战利品转移面板

#### 宝箱类型

| 类型 | 外观 | 物品数 | 常见掉落 |
|------|------|--------|---------|
| **Crate** | 木箱/塑料箱 | 3~6 件 | 弹药、医疗、杂物 |
| **DuffleBag** | 行李袋 | 2~4 件 | 贵重品、杂物 |
| **WeaponCase** | 武器箱 | 1~2 件 | 武器、弹药 |

#### 创建宝箱蓝图：`BP_SearchableContainer`

1. Content Browser 右键 → Blueprint Class → 父类 **Actor** → 命名 `BP_SearchableContainer`
2. 添加组件：
   - **Static Mesh**（Root）— 代表容器外观
   - **Sphere Collision**（命名 `SearchTrigger`）— 半径 200，`OverlapAllDynamic`
3. 添加变量（勾选 Instance Editable）：
   - `ContainerType` : EContainerType
   - `SearchTime` : Float（默认 2.0 秒）
   - `SpawnedLoot` : FInventorySlot（Array）— 本次随机生成的物品
   - `HasBeenLooted` : Boolean
   - `IsBeingSearched` : Boolean
   - `SearchProgress` : Float（0→1）
4. Event Graph 逻辑：

```
Event BeginPlay:
  → 随机生成战利品（从 DataTable 按 SpawnChance 权重抽选）
  → 构造 FInventorySlot 数组 → 存入 SpawnedLoot

On Component Begin Overlap (SearchTrigger):
  → Cast to BP_PlayerCharacter
  → Set "CurrentContainer" on Player = self
  → Set "CanSearch" on Player = true

On Component End Overlap (SearchTrigger):
  → Cast to BP_PlayerCharacter
  → Set "CanSearch" on Player = false
```

#### 搜索机制

在 `BP_PlayerCharacter` 新增：
- 变量：`CanSearch` : Boolean、`IsSearching` : Boolean、`CurrentContainer` : BP_SearchableContainer（引用）
- 输入：新增 Input Action `IA_Search` → 绑定 **F 键**

```
按住 F (IA_Search → Started):
  → Branch (CanSearch && CurrentContainer != None)
    [True] → CurrentContainer → StartSearch
    → 屏幕显示搜索进度条（WBP_SearchProgress）

松开 F (IA_Search → Completed):
  → 如果进度 < 1.0 → 取消搜索，进度归零
  → 如果进度 >= 1.0 → 打开 WBP_ContainerLoot 面板

StartSearch 逻辑:
  用 Timeline 或 Tick 累加 SearchProgress
  进度条实时更新
  满 1.0 → OnSearchComplete
```

#### 战利品面板：`WBP_ContainerLoot`

```
Canvas Panel
├── Border [半透明黑底]
├── TextBlock [顶部] — "Crate" / "Duffle Bag" / "Weapon Case"
├── Wrap Box [左半边] — 容器物品格
│   └── 图标 + 名称 + 数量（点击移到背包）
├── Wrap Box [右半边] — 玩家背包格
│   └── 当前背包物品
├── Button "全部拾取" [底部左]
└── Button "关闭" [底部右]
```

交互规则：
- 点击容器中物品 → 移到背包
- 背包满时提示"背包已满"
- 关闭面板不拿走物品 → 物品留在容器里
- 容器空了 → 自动销毁（HasBeenLooted = true）

#### 数据表：`DT_ContainerLoot`

Content Browser 右键 → Miscellaneous → **Data Table** → Row Structure 选 `FContainerLootEntry`

填入示例数据（约 12-15 行），包括弹药、医疗、武器、贵重品、杂物各类。

---

## Day 3: 武器系统（装备 + 射击 + 伤害）

**目标：** 把捡到的武器拿在手上，左键射击，子弹命中造成伤害

### 3.1 创建武器基类 (30分钟)

1. Content Browser 右键 → Blueprint Class → 父类 **Actor** → 命名 `BP_WeaponBase`
2. 添加组件：
   - **Skeletal Mesh**（命名 `WeaponMesh`）— Root，给一个临时 Cube
   - **Arrow**（命名 `MuzzlePoint`）— 枪口位置，放在 Mesh 前段
3. 添加变量：
   - `WeaponData` : FWeaponData（勾选 Instance Editable）
   - `CurrentAmmo` : Integer（默认 30）
   - `OwnerCharacter` : Character（引用）

**射击蓝图逻辑（自定义事件 `Fire`）：**
```
Custom Event: Fire
  → Branch (CurrentAmmo > 0)
    [True]:
      → Get MuzzlePoint → GetWorldLocation → 存为 StartLoc
      → Get OwnerCharacter → GetActorForwardVector × WeaponData.Range → + StartLoc → 存为 EndLoc
      → LineTraceByChannel (Start, End, Visibility)
        [Hit]:
          → Break HitResult → HitActor
          → Cast to BP_EnemyCharacter（先留空，Day 4 补）
            → 调用 Enemy 的 "TakeDamage" 事件
          → 生成枪口特效（先留空）
          → SpawnEmitterAtLocation (先占位)
        [No Hit]: → 不做处理
      → CurrentAmmo -= 1
      → 广播 "OnAmmoChanged" 事件（给 HUD 用）
    [False]:
      → 播放空仓音效 / Print String "没子弹了!"
```

### 3.2 武器装备系统 (30分钟)

1. 打开 `BP_PlayerCharacter` 
2. 添加组件：
   - **Skeletal Mesh**（命名 `WeaponAttachPoint`）→ 挂到 `hand_r` socket 或直接挂 root
   - 在角色骨骼上创建一个 Socket：打开角色的 Skeletal Mesh → 右键 hand_r → Add Socket → 命名 `WeaponSocket`
3. 添加变量：
   - `EquippedWeapon` : BP_WeaponBase（引用）
   - `HasWeapon` : Boolean
4. 添加自定义事件 `EquipWeapon`：
```
Custom Event: EquipWeapon
  Inputs: WeaponPickup (BP_WeaponPickup)
  → 如果 HasWeapon == true → 先把当前武器 Destroy
  → SpawnActor (WeaponPickup.WeaponClass) → 存为 NewWeapon
  → AttachActorToComponent (NewWeapon, WeaponAttachPoint, "WeaponSocket")
  → Set EquippedWeapon = NewWeapon
  → Set HasWeapon = true
```

### 3.3 射击输入绑定 (15分钟)

1. Project Settings → Input → Action Mappings:
   - `Fire` → **Left Mouse Button**
   - `Reload` → **R**
   - `Aim` → **Right Mouse Button**（可选）

2. 在 `BP_PlayerCharacter` Event Graph:
```
EnhancedInputAction (IA_Fire) → Started
  → Branch (HasWeapon)
    [True] → EquippedWeapon → Call "Fire"

EnhancedInputAction (IA_Fire) → Triggered（持续按住）
  → 同上（实现连发）

EnhancedInputAction (IA_Reload) → Started
  → Branch (HasWeapon)
    [True]: → EquippedWeapon → Custom Event "Reload"
      → Set CurrentAmmo = WeaponData.MagazineSize
```

### 3.4 伤害/血量系统 (15分钟)

1. 创建 **Actor Component** 蓝图 → 命名 `AC_HealthComponent`
2. 添加变量：
   - `MaxHealth` : Float (默认 100)
   - `CurrentHealth` : Float (默认 100)
   - `IsDead` : Boolean
3. 自定义事件 `TakeDamage`：
```
Custom Event: TakeDamage
  Inputs: DamageAmount (Float)
  → CurrentHealth -= DamageAmount
  → Branch (CurrentHealth <= 0)
    [True]: → IsDead = true → Broadcast "OnDeath" Event
    [False]: → Broadcast "OnDamaged" Event
  → Broadcast "OnHealthChanged" Event
```
4. 给 `BP_PlayerCharacter` 添加 `AC_HealthComponent` 组件
5. 武器 `Fire` 事件里，LineTrace Hit 后 → 取 HitActor → 找 `AC_HealthComponent` → 调 `TakeDamage`

### 3.5 验证

1. Play → 捡起武器（Day2 拾取逻辑触发 EquipWeapon）
2. 按左键 → 看到 `CurrentAmmo` 减少
3. 暂时没有敌人，用 Print String 验证 LineTrace 命中

---

## Day 4: AI 敌人（巡逻 + 发现玩家 + 追击 + 攻击）

**目标：** 场景中有会巡逻的敌人，看到玩家后追过来开枪

### 4.1 创建敌人角色 (30分钟)

1. Content Browser 右键 → Blueprint Class → 父类 **Character** → 命名 `BP_EnemyCharacter`
2. 添加组件：
   - `AC_HealthComponent`（复用 Day 3 的）
   - **Skeletal Mesh** → 给 Mixamo 下载的角色模型（或临时用 Cube）
3. 添加变量：
   - `PatrolPoints` : Vector (Array) — 巡逻路径点
   - `CurrentPatrolIndex` : Integer
   - `DetectionRange` : Float (默认 2000)
   - `AttackRange` : Float (默认 1500)
   - `TargetPlayer` : Actor (引用)
   - `EnemyState` : Enum (Idle/Patrol/Chase/Attack/Dead)
4. 添加自定义事件 `ReceiveDamage`（被武器击中时调用）

### 4.2 创建 AI 枚举与结构 (5分钟)

1. Content Browser 右键 → Blueprints → **Enumeration** → 命名 `E_AIState`
2. 添加枚举值：`Idle`、`Patrol`、`Investigating`、`Chasing`、`Attacking`、`Dead`

### 4.3 简化 AI 方案（纯蓝图，推荐初学者）

> 行为树方案详见完整计划的 4.3 节。这里推荐初学者使用更直观的 Tick AI。

在 `BP_EnemyCharacter` 的 Event Graph：

```
Event Tick (DeltaSeconds)
  → Sequence:
    1. [检测玩家] GetAllActorsOfClass (BP_PlayerCharacter) → Get(0)
       → GetDistanceTo → <= DetectionRange ?
         [是] → 射线检测(Start=自己位置, End=玩家位置)
           → 没被遮挡 → Set TargetPlayer = 玩家
         [否] → Set TargetPlayer = None

    2. [行为切换]
       → Switch on (EnemyState)
         
         [Patrol]:
           → AI MoveTo (PatrolPoints[CurrentPatrolIndex])
           → On Success → CurrentPatrolIndex = (CurrentPatrolIndex+1) % 长度
           → 如果 TargetPlayer 有效 → EnemyState = Chase
           
         [Chase]:
           → AI MoveTo (TargetPlayer位置)
           → 距离 < AttackRange → EnemyState = Attack
           → 距离 > DetectionRange × 1.5 → EnemyState = Patrol（脱战）
           
         [Attack]:
           → Face 朝向 TargetPlayer（用 SetActorRotation + FindLookAtRotation）
           → 每 AttackCooldown 秒: 
             → LineTrace 打向 TargetPlayer
             → Hit → 调用 Player 的 AC_HealthComponent → TakeDamage(15)
```

### 4.4 放敌人到关卡 + 设巡逻路线 (15分钟)

1. 拖 `BP_EnemyCharacter` 到关卡
2. 选中敌人 → 细节面板 → PatrolPoints → 添加 3-4 个 Vector
3. 确保关卡中有 **NavMeshBoundsVolume**（AI 寻路需要），放大覆盖整个可玩区域后按 P 键查看绿色区域

### 4.5 验证

1. Play → 在远处能看到敌人在巡逻
2. 靠近 → 敌人追过来
3. 再靠近 → 敌人攻击，玩家血量下降

---

## Day 5: 撤离系统 + GameMode 胜负逻辑

**目标：** 到达撤离点 → 撤离倒计时 → 胜利；玩家死亡 → 失败

### 5.1 创建撤离点 (15分钟)

1. 右键 → Blueprint Class → Actor → 命名 `BP_ExtractionZone`
2. 添加组件：
   - **Box Collision**（Root，设为 400×400×400）
   - **Static Mesh**（一个显眼的平面/平台）
   - **Particle System**（暂时用 Niagara 或空着，做绿色信号标记）
3. 添加变量：
   - `ExtractionTime` : Float (默认 5.0 秒撤离读条)
   - `IsExtracting` : Boolean

**蓝图逻辑（Event Graph）：**
```
On Component Begin Overlap (Box)
  → Cast to BP_PlayerCharacter
    → Branch (Player 还活着)
      [True]: → Set IsExtracting = true
               → Call "StartExtraction" (自定义事件)
               → Delay (ExtractionTime) 
                 → Call GameMode → "PlayerExtracted" 事件
                 → 播放胜利 UI

On Component End Overlap (Box)
  → If IsExtracting:
    → Set IsExtracting = false
    → 取消撤离倒计时（用 DoOnce 或 Flag 控制）
```

### 5.2 创建 GameMode (20分钟)

1. Content Browser 右键 → Blueprint Class → 父类 **Game Mode Base** → 命名 `GM_Extraction`
2. 添加变量：
   - `RemainingTime` : Float (默认 1800 秒 = 30分钟)
   - `ExtractionCountdown` : Float (默认 5 秒)
   - `IsGameOver` : Boolean
3. 事件图：

```
Event BeginPlay:
  → Set RemainingTime = 1800

Event Tick:
  → RemainingTime -= DeltaSeconds
  → Branch (RemainingTime <= 0)
    [True] → Call "GameOver" (失败 — 时间到)

Custom Event: PlayerExtracted:
  → Call "GameOver" (胜利)

Custom Event: PlayerDied:
  → Call "GameOver" (失败)

Custom Event: GameOver:
  Input: bWin (Boolean)
  → Set IsGameOver = true
  → 创建 WBP_GameResult Widget
  → Add to Viewport
  → Set Input Mode UI Only
  → Pause Game (SetGamePaused = true, 可选)
```

4. **在关卡设置中覆盖 GameMode：**
   - 打开 `L_Extraction`
   - World Settings → GameMode Override → 选 `GM_Extraction`
   - 保存

### 5.3 连接死亡 → GameOver (5分钟)

在 `BP_PlayerCharacter` → `AC_HealthComponent` → `OnDeath` 事件：
```
AC_HealthComponent → Event OnDeath:
  → GetGameMode → Cast to GM_Extraction
  → Call "PlayerDied"
```

### 5.4 验证

1. Play → 跑到撤离点 → 看到进度提示（临时用 Print String）
2. 倒计时结束 → 提示胜利
3. 被敌人打死 → 提示失败
4. 时间耗尽 → 提示失败

---

## Day 6: UI/HUD（血量、弹药、背包、结果画面）

**目标：** 屏幕上有完整的玩家状态信息

### 6.1 创建 HUD (45分钟)

1. Content Browser 右键 → User Interface → **Widget Blueprint** → 命名 `WBP_HUD`
2. 设计布局（拖控件到 Canvas Panel）：

```
Canvas Panel (Root)
├── Horizontal Box [左上角] — 血量区域
│   ├── TextBlock "HP:"
│   └── ProgressBar (绑定血量百分比) — 命名 PB_Health
│
├── Vertical Box [右上角] — 武器/弹药
│   ├── TextBlock "武器: " + TextBlock(绑定武器名) — 命名 TXT_WeaponName
│   └── TextBlock "弹药: " + TextBlock(绑定弹药) — 命名 TXT_Ammo
│
├── TextBlock [底部居中] — 交互提示
│   └── "按 E 拾取" (默认隐藏，可交互时显示) — 命名 TXT_InteractPrompt
│
├── TextBlock [顶部居中] — 倒计时
│   └── "剩余时间: 05:00" — 命名 TXT_Timer
│
└── TextBlock [底部居中] — 撤离进度
    └── "撤离中... 3s" (默认隐藏) — 命名 TXT_ExtractionProgress
```

3. **数据绑定（绑定蓝图变量/函数）：**

在 WBP_HUD 的 Event Graph：
```
Event Construct:
  → Get Owning Player Pawn → Cast to BP_PlayerCharacter
  → 存为 "PlayerRef" 变量
  → 绑定事件: PlayerRef → AC_HealthComponent → OnHealthChanged
    → Update HealthBar
  → 绑定事件: PlayerRef → EquippedWeapon → OnAmmoChanged
    → Update AmmoText

Event Tick:
  → 更新 TXT_Timer = Format Text(GetGameMode → Get RemainingTime)
  → 更新 TXT_InteractPrompt (根据 CanInteract)
  → 更新 TXT_ExtractionProgress (根据是否在撤离区域)
```

### 6.2 创建背包 UI (30分钟)

1. Content Browser 右键 → Widget Blueprint → 命名 `WBP_Inventory`
2. 设计布局：

```
Canvas Panel
├── Border [背景] — 半透明黑底
├── TextBlock "背包" [顶部居中]
├── Wrap Box [网格排列物品格]
│   └── 每个格: WBP_InventorySlot（子 Widget）
│       ├── Image (物品图标)
│       ├── TextBlock (物品名)
│       └── TextBlock (数量)
└── Button "关闭" [右上角]
```

3. 打开背包逻辑：
```
BP_PlayerCharacter → 绑定按键 Tab → 
  → Create Widget (WBP_Inventory)
  → Add to Viewport
  → Set Input Mode Game and UI
  → Show Mouse Cursor

关闭逻辑 → Remove from Parent → Set Input Mode Game Only → Hide Mouse Cursor
```

### 6.3 创建结果画面 (15分钟)

1. Widget Blueprint → 命名 `WBP_GameResult`
2. 布局：
```
Canvas Panel
├── Border [半透明黑背景]
├── TextBlock [居中] — 绑定结果文字（撤离成功/死亡/时间耗尽）
│   命名 TXT_ResultTitle
├── TextBlock [居中偏下] — 显示统计数据
│   命名 TXT_Stats
├── Button "重新开始" 
│   OnClicked → OpenLevel (L_Extraction)
└── Button "退出" 
    OnClicked → Quit Game
```

### 6.4 给 Player 创建 HUD (5分钟)

在 `BP_PlayerCharacter` → Event BeginPlay:
```
Event BeginPlay:
  → Create Widget (WBP_HUD)
  → Add to Viewport
  → Store as "HUDRef" variable
```

### 6.5 验证

1. Play → 左上角血量条可见
2. 捡起武器 → 右上角弹药显示
3. 走近物品 → 底部出现 "按 E 拾取"
4. 按 Tab → 背包打开/关闭
5. 撤离/死亡 → 结果画面出现

---

## Day 7: 打磨 + 音效 + 打包测试

**目标：** 修复明显 bug，加基础音效，做一个可运行的包

### 7.1 Bug 修复清单 (60分钟)

逐项测试：
- [ ] 捡起多个物品，背包满了怎么办？（加判断 `Inventory.Num() < MaxSlots`）
- [ ] 武器没子弹了按射击 → 要有反馈（干火音效或提示）
- [ ] 敌人死后还会不会追？（OnDeath → Set AIState = Dead → 停止 Tick 逻辑）
- [ ] 撤离时被打断（走出区域）→ 倒计时是否重置？
- [ ] 切换关卡后会不会有残留 UI？（Event EndPlay → Remove All Widgets）
- [ ] 敌人看到玩家但有障碍物 → 射线检测是否生效？

### 7.2 基础音效 (30分钟)

1. 从免费音效网站（freesound.org / zapsplat.com）下载：
   - 枪声（手枪、步枪）
   - 拾取音效
   - 脚步声（暂时跳过，太复杂）
   - 撤离成功音效
   - 受伤/死亡音效
2. 导入：Content Browser → 拖 WAV/MP3 文件进来
3. 在对应蓝图里添加：
   - `BP_WeaponBase::Fire` → **Play Sound at Location**（枪口位置）
   - `BP_PickupBase::BeginOverlap → 拾取成功时` → **Play Sound 2D**
   - `AC_HealthComponent::TakeDamage` → **Play Sound 2D**（受伤叫声）
   - `GM_Extraction::PlayerExtracted` → **Play Sound 2D**（胜利音）

### 7.3 简单特效 (30分钟)

1. Niagara 粒子系统：
   - 右键 → FX → **Niagara System** → 用模板 `Simple Sprite Burst`
   - 枪口闪光：放在 `BP_WeaponBase::Fire` 里 Spawn
   - 命中特效：LineTrace Hit Location → Spawn Niagara

### 7.4 打包测试 (30分钟)

1. 菜单栏 → Platforms → Windows → **Package Project**
2. 选输出目录 → 开始打包
3. 打包完成后运行 exe 测试：
   - 检查独立运行时是否正常
   - 检查帧率
   - 检查 UI 是否适配屏幕

### 7.5 如果还有时间 (30分钟)

- 给敌人加简单的死亡动画（Ragdoll: SetAllBodiesSimulatePhysics）
- 添加小地图（SceneCaptureComponent2D + RenderTarget）
- 添加更多武器类型
- 添加多个敌人 + 不同巡逻路线

---

## 每日时间分配

| 天 | 重点 | 预计时间 |
|----|------|---------|
| Day 1 | 项目创建 + 地形 + 角色移动 | 1.5h |
| Day 2 | 拾取系统 + 背包 + 宝箱搜索 | 3h |
| Day 3 | 武器系统（射击+伤害） | 2.5h |
| Day 4 | AI 敌人 | 2.5h |
| Day 5 | 撤离点 + GameMode | 2h |
| Day 6 | HUD/UI 全套 + 战利品面板 | 2.5h |
| Day 7 | 打磨 + 音效 + 打包 | 2h |
| **总计** | | **~16h** |

---

## 免费素材来源

| 类型 | 来源 |
|------|------|
| 角色模型/动画 | Mixamo.com（Adobe 免费） |
| 武器模型 | Epic Marketplace → 搜 "Free Weapon" |
| 环境资产 | Epic Marketplace → "Free" 筛选 → "Modular Neighborhood Pack" |
| 音效 | freesound.org / zapsplat.com |
| 图标 | game-icons.net（CC 许可） |
| UI 素材 | 自己用 Figma 画简单方框 / 用 UE 自带 |

---

## 前置工作清单（现在就可以做）

1. [ ] 启动 Epic Launcher → 安装 UE 5.4 或 5.5
2. [ ] 在 Epic Marketplace 领取免费资产（搜 "Free" + "Permanent Collection"）
3. [ ] 注册 Mixamo 账号
4. [ ] 下载 Visual Studio 2022（如果没装，勾选 "Game Development with C++" workload）
5. [ ] 在 UE Marketplace 下载 "Content Examples" 项目（学习参考用）

---

## 遇到问题时的排查顺序

1. **蓝图编译报错** → 检查变量类型是否匹配，Cast 是否成功
2. **角色不能动** → 检查 PlayerController 和 GameMode 是否正确指定
3. **碰撞不触发** → 检查 Collision Preset 是否设置 Overlap
4. **AI 不动** → 检查 NavMeshBoundsVolume 是否覆盖了地图
5. **UI 不显示** → 检查 AddToViewport 是否被调用，ZOrder 是否被遮挡

---

## 完整体验流程（塔科夫风格）

### 一场典型的战局

```
你出生在地图边缘 → 手里没武器
    ↓
跑向最近的建筑物/掩体区域
    ↓ 发现地面拾取物
按 E 捡起一把手枪 → 自动装备到手上
    ↓ 发现一个木箱
按住 F 搜索 → 进度条走 2 秒 → 打开战利品面板
    → 箱子里有: 5.56弹药×45、绷带×2、金链×1
    → 点击物品拖入背包（12格背包管理）
    ↓ 搜索第二个箱子（行李袋）
    → 里面只有胶带×2
    ↓ 听到脚步声，AI 巡逻敌人靠近
发现敌人 → AI 转为追击 → 你开火
    → 击毙敌人，敌人尸体旁什么都没有（简化处理）
    ↓ 血量只剩 30
使用背包里的绷带 → 血量 +25
    ↓ 时间已过 20 分钟，倒计时显示 10:00
决定撤离
    → 跑到撤离点 A → 提示"需要特殊钥匙"
    → 继续找撤离点 B → 免费撤离点
    → 走进绿光区域 → "撤离中... 5, 4, 3, 2, 1"
    ↓
画面变黑 → "撤离成功！"
    → 显示本次战利品清单: 金链、5.56弹药、胶带...
    → 按钮: [重新开始] [退出]
```

### 与塔科夫的对应

| 塔科夫 | 本项目 |
|--------|--------|
| SCAV 巡逻 | BP_EnemyCharacter Tick AI 巡逻+追击+攻击 |
| 搜刮容器 | 按住 F 搜索木箱/行李袋/武器箱 |
| 背包管理 | 12格背包，战利品面板拖拽转移 |
| 贵重品 | Valuables 分类（金链/比特币等） |
| 多撤离点 | 2~3 个撤离点，部分有条件限制 |
| 30分钟限时 | 倒计时 1800 秒 |
| 死亡丢装备 | 死亡 → GameOver 失败，战利品丢失 |
| 保险/藏身处 | 不做（需求太大） |
| 商人/跳蚤市场 | 不做（单机没必要） |

---

## 验证方式

每天结束时按「验证」步骤执行 Play 测试，确认当天目标功能的行为是否正确。Day 7 打包后在独立 exe 中跑一遍完整游戏循环。
