# 搜打撤游戏 - 7天实施计划

## Context

在 UE5 中从零构建一个单机"搜刮-战斗-撤离"游戏。目标是 7 天内完成可玩的核心循环：玩家在小地图中出生 → 搜刮武器/物资 → 与 AI 敌人交战 → 到达撤离点获胜。

**开发方式:** 蓝图为主，少量 C++（仅在蓝图无法优雅实现的场景写 C++，如 Struct/Enum 定义、性能敏感的检测逻辑）  
**开发者水平:** 初学者  
**素材方案:** Epic 免费商城资源 + Mixamo 动画  
**UE 版本:** 5.7（使用增强输入 Enhanced Input，无旧版 Action Mappings）

---

## 总体架构

```
Contents/
├── Core/
│   ├── Enums/E_WeaponType        — 武器类型枚举
│   ├── Structs/S_InventoryItem   — 背包物品结构体
│   └── GameMode/GM_Extraction    — 撤离游戏模式
├── Character/
│   ├── BP_PlayerCharacter        — 玩家角色 (Character)
│   ├── BP_EnemyCharacter         — 敌人角色 (Character)
│   └── AC_HealthComponent        — 血量组件 (ActorComponent)
├── Items/
│   ├── BP_PickupBase             — 拾取物基类
│   ├── BP_WeaponPickup           — 武器拾取物
│   ├── BP_AmmoPickup             — 弹药拾取物
│   ├── BP_HealthPickup           — 医疗拾取物
│   ├── BP_SearchableContainer    — 可搜索宝箱
│   └── DT_ContainerLoot          — 宝箱掉落数据表
├── Weapons/
│   ├── BP_WeaponBase             — 武器基类
│   ├── BP_Weapon_Rifle           — 步枪
│   └── BP_Weapon_Pistol          — 手枪
├── AI/
│   ├── BT_Enemy                  — 行为树
│   ├── BB_Enemy                  — 黑板
│   └── BT_Task_*                 — 自定义任务节点
├── UI/
│   ├── WBP_HUD                   — 主 HUD
│   ├── WBP_Inventory             — 背包面板
│   └── WBP_GameResult            — 胜负结果
└── Maps/
    └── L_ExtractionTest          — 测试关卡
```

---

## Day 1: 项目创建 + 角色移动 + 基础相机

**目标：** 能在一个地形上走路、跑步、跳跃、转动视角

### 1.1 创建项目 (10分钟)

1. 打开 Epic Games Launcher → 启动 UE5.4/5.5
2. 选择 **Games** → **Third Person** 模板
3. 项目设置：
   - Blueprint: ✓（不要 C++ 模板，之后单独加 C++ 类）
   - 包含初学者内容包: ✓
   - 光线追踪: 关闭（初学者性能友好）
   - 项目名: `ExtractionGame`
4. 创建后先别动，等 Shader 编译完成（右下角进度条消失）

### 1.2 熟悉已有内容 (15分钟)

1. 模板自带 `BP_ThirdPersonCharacter` — 这就是你的玩家角色，已含移动/跳跃/相机
2. 按 **Alt+P** 或点工具栏 Play 按钮进入 PIE（Play In Editor）
3. 确认 WASD 移动、空格跳跃、鼠标转视角正常
4. 退出 PIE

### 1.3 创建基础关卡 (20分钟)

1. 内容浏览器(Content Browser) → Content 文件夹右键 → 新建文件夹 → 命名 `Maps`
2. 在 `Maps` 右键 → 关卡 → 命名 `L_Extraction`
3. 双击 `L_Extraction` 打开空关卡
4. 从 Place Actors 面板拖入：
   - **定向光源(Directional Light)** — 主光源
   - **天空光照(Sky Light)** — 环境光
   - **天空大气(Sky Atmosphere)** — 搜索栏输入名拖入
   - **体积云(Volumetric Cloud)** — 云
   - **玩家出生点(Player Start)** — 放在(0,0,0)，玩家出生点
5. 选中 Directional Light → 细节(Details)面板 → Transform → Rotation → 设为 `(-60, -45, 0)` 让光有角度
6. **Ctrl+S** 保存

### 1.4 添加简易地形 (15分钟)

1. 顶部工具栏 → 模式选择 → **地形(Landscape)**（山形图标）
2. 默认参数就行 → 点 **创建(Create)** 按钮
3. 创建后在 Landscape 模式下选 **雕刻(Sculpt)** 工具 → 刷一个略有起伏的地面
4. 不用太细，有地面能走就行
5. 回到 Selection 模式（箭头图标）

### 1.5 配置 GameMode 覆盖 (5分钟)

1. 打开 `L_Extraction` 关卡
2. 菜单栏 → Settings → **World Settings**
3. World Settings 面板 → GameMode Override → 选 `BP_ThirdPersonGameMode`（模板自带）
4. **Ctrl+S** 保存

### 1.6 验证 (5分钟)

1. 点击 Play → 角色能在地形上走动 → Day 1 完成

---

## Day 2: 拾取系统 + 背包

**目标：** 场景中的物品可以按 E 拾取，打开背包能看到

### 2.1 创建数据结构 (C++ 方式，更稳定)

> 注意：如果不想碰 C++，这一步可以完全用 Blueprint Struct 代替。路径：Content Browser 右键 → Blueprints → Structure。  
> 但 C++ 的 Enum/Struct 在后续蓝图节点中引用更稳定，推荐花 15 分钟做这个。

1. 编辑器菜单 → Tools → **New C++ Class**
2. 父类选择 **None**（直接创建头文件）→ 实际选 `UObject` 就行
3. 命名为 `ExtractionTypes` → 创建
4. Visual Studio 自动打开，替换文件内容为：

```cpp
// ExtractionTypes.h
#pragma once
#include "CoreMinimal.h"
#include "ExtractionTypes.generated.h"

UENUM(BlueprintType)
enum class EWeaponType : uint8
{
    Pistol      UMETA(DisplayName = "手枪"),
    Rifle       UMETA(DisplayName = "步枪"),
    Shotgun     UMETA(DisplayName = "霰弹枪")
};

UENUM(BlueprintType)
enum class EItemCategory : uint8
{
    Weapon      UMETA(DisplayName = "武器"),
    Ammo        UMETA(DisplayName = "弹药"),
    Health      UMETA(DisplayName = "医疗"),
    Misc        UMETA(DisplayName = "杂项")
};

USTRUCT(BlueprintType)
struct FInventorySlot
{
    GENERATED_BODY()

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    FName ItemID;

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    FText ItemName;

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    EItemCategory Category;

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    int32 Quantity = 0;

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    UTexture2D* Icon = nullptr;
};

USTRUCT(BlueprintType)
struct FWeaponData : public FTableRowBase
{
    GENERATED_BODY()

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    FText WeaponName;

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    EWeaponType WeaponType;

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    float BaseDamage = 10.0f;

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    float FireRate = 0.1f;  // 秒/发

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    int32 MagazineSize = 30;

    UPROPERTY(EditAnywhere, BlueprintReadWrite)
    float Range = 10000.0f;
};
```

5. 编译 (Ctrl+Alt+F11 或点击工具栏 Compile 按钮)
6. 关闭 VS，回到编辑器，等待 Hot Reload 完成

### 2.2 创建拾取物基类 (蓝图) (30分钟)

1. 内容浏览器(Content Browser) → Content 右键 → 蓝图类(Blueprint Class) → 父类 **Actor** → 命名 `BP_PickupBase`
2. 双击打开蓝图编辑器
3. 添加组件（左上角 + 添加(Add Component)）：
   - **静态网格体(Static Mesh)**（命名 `PickupMesh`）— 设为根(Root)
   - **球体碰撞(Sphere Collision)**（命名 `InteractionTrigger`）
     - 半径设为 150
     - 碰撞预设(Collision Presets) → `OverlapAllDynamic`（全部动态重叠）
     - 在 细节(Details) → 事件(Events) → 勾选 `组件开始重叠时(On Component Begin Overlap)` 和 `组件结束重叠时(On Component End Overlap)`
4. 添加变量（左侧 我的蓝图(My Blueprint) → 变量(Variables) → +）：
   - `ItemID` : 名称(Name)
   - `ItemName` : 文本(Text)
   - `Category` : EItemCategory
   - `Quantity` : 整数(Integer)（默认 1）
   - 每个变量勾选 **实例可编辑(Instance Editable)** + **生成时公开(Expose on Spawn)**
5. 编译(Compile) → 保存(Save)

**蓝图逻辑（事件图表(Event Graph)）：**

```
组件开始重叠时(OnComponentBeginOverlap) [InteractionTrigger]
  其他Actor → 转换为(Cast To) BP_ThirdPersonCharacter
    → 分支(Branch)
      True → 设置 CanInteract（目标=As BP Third Person Character, 值=true）
           → 设置 CurrentInteractTarget（目标=As BP Third Person Character, 值=self）
      False → (不连)

组件结束重叠时(OnComponentEndOverlap) [InteractionTrigger]
  其他Actor → 转换为(Cast To) BP_ThirdPersonCharacter
    → 分支(Branch)
      True → 设置 CanInteract（目标=As BP Third Person Character, 值=false）
           → 设置 CurrentInteractTarget（目标=As BP Third Person Character, 值=None）
```

### 2.3 给玩家角色加交互系统 (30分钟)

1. 打开 `BP_ThirdPersonCharacter`（模板自带）
2. 添加变量（我的蓝图 → 变量 → +）：
   - `CanInteract` : 布尔(Boolean)（默认 false）
   - `CurrentInteractTarget` : BP_PickupBase（对象引用(Object Reference)类型）
   - `Inventory` : InventorySlot（数组(Array)，蓝图里搜的时候不带 F 前缀）
   - `MaxInventorySlots` : 整数(Integer)（默认 12）
3. 在事件图表中添加键盘输入：

**UE 5.7 使用增强输入(Enhanced Input)，旧版的 Project Settings → Input → Action Mappings 已不存在。**

**步骤 A — 创建输入动作(Input Action)：**
在内容浏览器中直接搜索 `IA_Interact`，该输入动作已创建好（值类型为 数字(布尔)(Digital (bool))）。

**步骤 B — 绑定按键到输入动作：**
打开 `Content/Input/IMC_Default`（输入映射上下文(Input Mapping Context)），直接搜索 `IA_Interact` 查看其按键绑定（已绑定 E 键）。

**步骤 C — 蓝图绑定逻辑：**
1. 打开 `BP_ThirdPersonCharacter` → 事件图表(Event Graph)
2. 空白处右键 → 直接搜索 `IA_Interact` → 自动创建已绑定该 Input Action 的 EnhancedInputAction 节点
3. 从 已开始(Started) 引脚连出交互逻辑：

> **注意：** 直接搜索 Input Action 资产名即可自动创建节点，无需先搜 "EnhancedInputAction" 再下拉选择。

```
增强型输入动作(EnhancedInputAction) (IA_Interact) → 已开始(Started)
  分支(Branch) (CanInteract)
    True:
      → 获取(Get) CurrentInteractTarget
      → 拆分(Break) 其变量 (ItemID, ItemName, Category, Quantity)
      → 构造(Make) InventorySlot（蓝图里搜 Make InventorySlot，不带 F 前缀）
      → 加入 Inventory 数组（数组 ADD）
      → 销毁Actor(DestroyActor) (CurrentInteractTarget)
      → 设置 CanInteract = false
```

> **提示：** 模板自带的 Jump/Look/Move 已经演示了 EnhancedInputAction 的绑定方式，直接模仿即可。

### 2.4 放几个拾取物到关卡 (10分钟)

1. 从 Content Browser 拖 `BP_PickupBase` 到关卡中 → 放 3-4 个
2. 每个设置不同的 ItemName（如 "弹药箱"、"医疗包"、"M4A1"）
3. 给 PickupMesh 选一个 Static Mesh（初学者内容包里挑 Shape_Sphere / Shape_Cube）
4. **Ctrl+S** 保存关卡

### 2.5 验证

1. Play → 走到物品旁看到交互提示（可以先不加 HUD，用 Print String 打印 `CanInteract`）
2. 按 E → 物品消失，Inventory 数组数量 +1
3. 用 Blueprint Debugger 或 Print String 查看 Inventory 内容

---

### Day 2 附加：可搜索宝箱系统（塔科夫风格）

**目标：** 地图上放置宝箱/行李袋/武器箱，按住 F 搜索读条，打开战利品转移面板

#### 宝箱类型

| 类型 | 外观 | 物品数 | 常见掉落 |
|------|------|--------|---------|
| **Crate** | 木箱/塑料箱 | 3~6 件 | 弹药、医疗、杂物 |
| **DuffleBag** | 行李袋 | 2~4 件 | 贵重品、杂物 |
| **WeaponCase** | 武器箱 | 1~2 件 | 武器、弹药 |

#### 创建宝箱蓝图：`BP_SearchableContainer`

1. 内容浏览器(Content Browser) 右键 → 蓝图类(Blueprint Class) → 父类 **Actor** → 命名 `BP_SearchableContainer`
2. 添加组件：
   - **静态网格体(Static Mesh)**（根(Root)）— 代表容器外观
   - **球体碰撞(Sphere Collision)**（命名 `SearchTrigger`）— 半径 200，`全部动态重叠(OverlapAllDynamic)`
3. 添加变量（勾选 实例可编辑(Instance Editable)）：
   - `ContainerType` : EContainerType
   - `SearchTime` : 浮点(Float)（默认 2.0 秒）
   - `SpawnedLoot` : InventorySlot（数组(Array)，蓝图里搜不带 F 前缀）— 本次随机生成的物品
   - `HasBeenLooted` : 布尔(Boolean)
   - `IsBeingSearched` : 布尔(Boolean)
   - `SearchProgress` : 浮点(Float)（0→1）
4. 事件图表(Event Graph) 逻辑：

```
事件 BeginPlay:
  → 随机生成战利品（从 DataTable 按 SpawnChance 权重抽选）
  → 构造 InventorySlot 数组（蓝图里搜 Make InventorySlot） → 存入 SpawnedLoot

组件开始重叠时(OnComponentBeginOverlap) (SearchTrigger):
  → 转换为(Cast To) BP_ThirdPersonCharacter
  → 设置(Set) CurrentContainer（目标=玩家, 值=self）
  → 设置(Set) CanSearch（目标=玩家, 值=true）

组件结束重叠时(OnComponentEndOverlap) (SearchTrigger):
  → 转换为(Cast To) BP_ThirdPersonCharacter
  → 设置(Set) CanSearch（目标=玩家, 值=false）
```

#### 搜索机制

在 `BP_ThirdPersonCharacter` 新增：
- 变量：`CanSearch` : 布尔(Boolean)、`IsSearching` : 布尔(Boolean)、`CurrentContainer` : BP_SearchableContainer（对象引用）
	- 输入：
	  在内容浏览器中直接搜索 `IA_Search`（已创建好，值类型为 数字(布尔)(Digital (bool))）。打开 `Content/Input/IMC_Default` 直接搜索 `IA_Search` 查看其按键绑定（已绑定 F 键）。

```
按住 F (IA_Search → 已开始(Started)):
  → 分支(Branch) (CanSearch && CurrentContainer != None)
    True → CurrentContainer → StartSearch
    → 屏幕显示搜索进度条（WBP_SearchProgress）

松开 F (IA_Search → 已完成(Completed)):
  → 如果进度 < 1.0 → 取消搜索，进度归零
  → 如果进度 >= 1.0 → 打开 WBP_ContainerLoot 面板

StartSearch 逻辑:
  用 时间轴(Timeline) 或 Tick 累加 SearchProgress
  进度条实时更新
  满 1.0 → OnSearchComplete
```

#### 战利品面板：`WBP_ContainerLoot`

```
画布面板(Canvas Panel)
├── 边框(Border) [半透明黑底]
├── 文本块(TextBlock) [顶部] — "木箱" / "行李袋" / "武器箱"
├── 包裹框(Wrap Box) [左半边] — 容器物品格
│   └── 图像(Image)图标 + 文本名称 + 数量（点击移到背包）
├── 包裹框(Wrap Box) [右半边] — 玩家背包格
│   └── 当前背包物品
├── 按钮(Button) "全部拾取" [底部左]
└── 按钮(Button) "关闭" [底部右]
```

交互规则：
- 点击容器中物品 → 移到背包
- 背包满时提示"背包已满"
- 关闭面板不拿走物品 → 物品留在容器里
- 容器空了 → 自动销毁（HasBeenLooted = true）

#### 数据表：`DT_ContainerLoot`

内容浏览器(Content Browser) 右键 → 杂项(Miscellaneous) → **数据表(Data Table)** → 行结构(Row Structure) 选 `ContainerLootEntry`（蓝图里搜不带 F 前缀）

填入示例数据（约 12-15 行），包括弹药、医疗、武器、贵重品、杂物各类。

---

## Day 3: 武器系统（装备 + 射击 + 伤害）

**目标：** 把捡到的武器拿在手上，左键射击，子弹命中造成伤害

### 3.1 创建武器基类 (30分钟)

1. 内容浏览器(Content Browser) 右键 → 蓝图类(Blueprint Class) → 父类 **Actor** → 命名 `BP_WeaponBase`
2. 添加组件：
   - **骨骼网格体(Skeletal Mesh)**（命名 `WeaponMesh`）— 根(Root)，给一个临时 Cube
   - **箭头(Arrow)**（命名 `MuzzlePoint`）— 枪口位置，放在 Mesh 前段
3. 添加变量：
   - `WeaponData` : WeaponData（蓝图变量类型，搜的时候不带 F 前缀，勾选 实例可编辑(Instance Editable)）
   - `CurrentAmmo` : 整数(Integer)（默认 30）
   - `OwnerCharacter` : Character（引用）

**射击蓝图逻辑（自定义事件(Custom Event) `Fire`）：**
```
自定义事件: Fire
  → 分支(Branch) (CurrentAmmo > 0)
    True:
      → 获取(Get) MuzzlePoint → 获取世界位置(GetWorldLocation) → 存为 StartLoc
      → 获取(Get) OwnerCharacter → 获取Actor前向矢量(GetActorForwardVector) × WeaponData.Range → + StartLoc → 存为 EndLoc
      → 按通道射线追踪(LineTraceByChannel) (Start, End, 可见性(Visibility))
        命中(Hit):
          → 拆分(Break) HitResult → HitActor
          → 转换为(Cast To) BP_EnemyCharacter（先留空，Day 4 补）
            → 调用 Enemy 的 "TakeDamage" 事件
          → 生成枪口特效（先留空）
          → 在位置生成发射器(SpawnEmitterAtLocation) (先占位)
        未命中(No Hit): → 不做处理
      → CurrentAmmo -= 1
      → 广播 "OnAmmoChanged" 事件（给 HUD 用）
    False:
      → 播放空仓音效 / 打印字符串(Print String) "没子弹了!"
```

### 3.2 武器装备系统 (30分钟)

1. 打开 `BP_ThirdPersonCharacter`
2. 添加组件：
   - **骨骼网格体(Skeletal Mesh)**（命名 `WeaponAttachPoint`）→ 挂到 `hand_r` socket 或直接挂 根(Root)
   - 在角色骨骼上创建一个 Socket：打开角色的 Skeletal Mesh → 右键 hand_r → 添加插槽(Add Socket) → 命名 `WeaponSocket`
3. 添加变量：
   - `EquippedWeapon` : BP_WeaponBase（对象引用(Object Reference)）
   - `HasWeapon` : 布尔(Boolean)
4. 添加自定义事件(Custom Event) `EquipWeapon`：
```
自定义事件: EquipWeapon
  输入(Inputs): WeaponPickup (BP_WeaponPickup)
  → 如果 HasWeapon == true → 先把当前武器 Destroy
  → 生成Actor(SpawnActor) (WeaponPickup.WeaponClass) → 存为 NewWeapon
  → 将Actor附加到组件(AttachActorToComponent) (NewWeapon, WeaponAttachPoint, "WeaponSocket")
  → 设置 EquippedWeapon = NewWeapon
  → 设置 HasWeapon = true
```

### 3.3 射击输入绑定 (15分钟)

**UE 5.7 使用增强输入(Enhanced Input)，没有 Action Mappings 面板。**

**步骤 A — 创建输入动作(Input Action)：**
1. 内容浏览器(Content Browser) 右键 → 输入(Input) → 输入动作(Input Action)
2. 分别创建：
   - `IA_Fire` → 值类型选 数字(布尔)(Digital (bool))
   - `IA_Reload` → 值类型选 数字(布尔)(Digital (bool))
   - `IA_Aim` → 值类型选 数字(布尔)(Digital (bool))（可选）

**步骤 B — 绑定按键（在 IMC_Default 中）：**
1. 打开 `Content/Input/IMC_Default`（输入映射上下文(Input Mapping Context)）
2. 在 **Default Key Mappings**（默认按键映射）区域：
   - `+` → 选 `IA_Fire` → 按键选 **鼠标左键(Left Mouse Button)**
   - `+` → 选 `IA_Reload` → 按键选 **R**
   - `+` → 选 `IA_Aim` → 按键选 **鼠标右键(Right Mouse Button)**（可选）
3. 保存

**步骤 C — 在 `BP_ThirdPersonCharacter` 事件图表(Event Graph) 中绑定：**
空白处右键 → 搜索 "EnhancedInputAction" → 新建节点 → 选对应的 Input Action → 从引脚连出：
```
增强型输入动作(EnhancedInputAction) (IA_Fire) → 已开始(Started)
  → 分支(Branch) (HasWeapon)
    True → EquippedWeapon → 调用 "Fire"

增强型输入动作(EnhancedInputAction) (IA_Fire) → 触发中(Triggered)（持续按住）
  → 同上（实现连发）

增强型输入动作(EnhancedInputAction) (IA_Reload) → 已开始(Started)
  → 分支(Branch) (HasWeapon)
    True: → EquippedWeapon → 自定义事件(Custom Event) "Reload"
      → 设置 CurrentAmmo = WeaponData.MagazineSize
```

### 3.4 伤害/血量系统 (15分钟)

1. 创建 **Actor组件(Actor Component)** 蓝图 → 命名 `AC_HealthComponent`
2. 添加变量：
   - `MaxHealth` : 浮点(Float)（默认 100）
   - `CurrentHealth` : 浮点(Float)（默认 100）
   - `IsDead` : 布尔(Boolean)
3. 自定义事件(Custom Event) `TakeDamage`：
```
自定义事件: TakeDamage
  输入(Inputs): DamageAmount (浮点 Float)
  → CurrentHealth -= DamageAmount
  → 分支(Branch) (CurrentHealth <= 0)
    True: → IsDead = true → 广播(Broadcast) "OnDeath" 事件
    False: → 广播(Broadcast) "OnDamaged" 事件
  → 广播(Broadcast) "OnHealthChanged" 事件
```
4. 给 `BP_ThirdPersonCharacter` 添加 `AC_HealthComponent` 组件
5. 武器 `Fire` 事件里，LineTrace 命中(Hit) 后 → 取 HitActor → 找 `AC_HealthComponent` → 调 `TakeDamage`

### 3.5 验证

1. Play → 捡起武器（Day2 拾取逻辑触发 EquipWeapon）
2. 按左键 → 看到 `CurrentAmmo` 减少
3. 暂时没有敌人，用 Print String 验证 LineTrace 命中

---

## Day 4: AI 敌人（巡逻 + 发现玩家 + 追击 + 攻击）

**目标：** 场景中有会巡逻的敌人，看到玩家后追过来开枪

### 4.1 创建敌人角色 (30分钟)

1. 内容浏览器(Content Browser) 右键 → 蓝图类(Blueprint Class) → 父类 **Character** → 命名 `BP_EnemyCharacter`
2. 添加组件：
   - `AC_HealthComponent`（复用 Day 3 的）
   - **骨骼网格体(Skeletal Mesh)** → 给 Mixamo 下载的角色模型（或临时用 Cube）
3. 添加变量：
   - `PatrolPoints` : 向量(Vector)（数组(Array)）— 巡逻路径点
   - `CurrentPatrolIndex` : 整数(Integer)
   - `DetectionRange` : 浮点(Float)（默认 2000）
   - `AttackRange` : 浮点(Float)（默认 1500）
   - `TargetPlayer` : Actor（对象引用(Object Reference)）
   - `EnemyState` : E_AIState 枚举
   - `AttackCooldown` : 浮点(Float)（默认 1.0）
4. 添加自定义事件(Custom Event) `ReceiveDamage`（被武器击中时调用）

### 4.2 创建 AI 枚举与结构 (5分钟)

1. 内容浏览器(Content Browser) 右键 → 蓝图(Blueprints) → **枚举(Enumeration)** → 命名 `E_AIState`
2. 添加枚举值：`Idle`、`Patrol`、`Investigating`、`Chasing`、`Attacking`、`Dead`

### 4.3 简化 AI 方案（纯蓝图，推荐初学者）

> 行为树方案详见完整计划的 4.3 节。这里推荐初学者使用更直观的 Tick AI。

在 `BP_EnemyCharacter` 的事件图表(Event Graph)：

```
事件 Tick (DeltaSeconds)
  → Sequence:
    1. [检测玩家] 获取类的所有Actor(GetAllActorsOfClass) (BP_ThirdPersonCharacter) → Get(0)
       → 获取距离到(GetDistanceTo) → <= DetectionRange ?
         是 → 射线检测(Start=自己位置, End=玩家位置)
           → 没被遮挡 → 设置 TargetPlayer = 玩家
         否 → 设置 TargetPlayer = None

    2. [行为切换]
       → 按枚举切换(Switch on) (EnemyState)
         
         巡逻(Patrol):
           → AI 移动到(AI MoveTo) (PatrolPoints[CurrentPatrolIndex])
           → 成功时(On Success) → CurrentPatrolIndex = (CurrentPatrolIndex+1) % 长度
           → 如果 TargetPlayer 有效 → EnemyState = Chase
           
         追击(Chase):
           → AI 移动到(AI MoveTo) (TargetPlayer位置)
           → 距离 < AttackRange → EnemyState = Attack
           → 距离 > DetectionRange × 1.5 → EnemyState = Patrol（脱战）
           
         攻击(Attack):
           → 朝向 TargetPlayer（用 设置Actor旋转(SetActorRotation) + 寻找看向的旋转(FindLookAtRotation)）
           → 每 AttackCooldown 秒:
             → 按通道射线追踪(LineTraceByChannel) 打向 TargetPlayer
             → 命中(Hit) → 调用 Player 的 AC_HealthComponent → TakeDamage(15)
```

### 4.4 放敌人到关卡 + 设巡逻路线 (15分钟)

1. 拖 `BP_EnemyCharacter` 到关卡
2. 选中敌人 → 细节(Details)面板 → PatrolPoints → 添加 3-4 个向量(Vector)
3. 确保关卡中有 **导航网格边界体积(NavMeshBoundsVolume)**（AI 寻路需要），放大覆盖整个可玩区域后按 P 键查看绿色区域

### 4.5 验证

1. Play → 在远处能看到敌人在巡逻
2. 靠近 → 敌人追过来
3. 再靠近 → 敌人攻击，玩家血量下降

---

## Day 5: 撤离系统 + GameMode 胜负逻辑

**目标：** 到达撤离点 → 撤离倒计时 → 胜利；玩家死亡 → 失败

### 5.1 创建撤离点 (15分钟)

1. 右键 → 蓝图类(Blueprint Class) → Actor → 命名 `BP_ExtractionZone`
2. 添加组件：
   - **盒体碰撞(Box Collision)**（根(Root)，设为 400×400×400）
   - **静态网格体(Static Mesh)**（一个显眼的平面/平台）
   - **粒子系统(Particle System)**（暂时用 Niagara 或空着，做绿色信号标记）
3. 添加变量：
   - `ExtractionTime` : 浮点(Float)（默认 5.0 秒撤离读条）
   - `IsExtracting` : 布尔(Boolean)

**蓝图逻辑（事件图表(Event Graph)）：**
```
组件开始重叠时(OnComponentBeginOverlap) (Box)
  → 转换为(Cast To) BP_ThirdPersonCharacter
    → 分支(Branch) (Player 还活着)
      True: → 设置 IsExtracting = true
            → 调用 "StartExtraction"（自定义事件）
            → 延迟(Delay) (ExtractionTime)
              → 调用 GameMode → "PlayerExtracted" 事件
              → 播放胜利 UI

组件结束重叠时(OnComponentEndOverlap) (Box)
  → 如果 IsExtracting:
    → 设置 IsExtracting = false
    → 取消撤离倒计时（用 DoOnce 或 Flag 控制）
```

### 5.2 创建 GameMode (20分钟)

1. 内容浏览器(Content Browser) 右键 → 蓝图类(Blueprint Class) → 父类 **游戏模式基类(Game Mode Base)** → 命名 `GM_Extraction`
2. 添加变量：
   - `RemainingTime` : 浮点(Float)（默认 1800 秒 = 30分钟）
   - `ExtractionCountdown` : 浮点(Float)（默认 5 秒）
   - `IsGameOver` : 布尔(Boolean)
3. 事件图表(Event Graph)：

```
事件 BeginPlay:
  → 设置 RemainingTime = 1800

事件 Tick:
  → RemainingTime -= DeltaSeconds
  → 分支(Branch) (RemainingTime <= 0)
    True → 调用 "GameOver"（失败 — 时间到）

自定义事件(Custom Event): PlayerExtracted:
  → 调用 "GameOver"（胜利）

自定义事件(Custom Event): PlayerDied:
  → 调用 "GameOver"（失败）

自定义事件(Custom Event): GameOver:
  输入(Input): bWin（布尔 Boolean）
  → 设置 IsGameOver = true
  → 创建控件(Create Widget) WBP_GameResult
  → 添加到视口(Add to Viewport)
  → 设置输入模式仅UI(Set Input Mode UI Only)
  → 暂停游戏(SetGamePaused = true, 可选)
```

4. **在关卡设置中覆盖 GameMode：**
   - 打开 `L_Extraction`
   - World Settings → GameMode Override → 选 `GM_Extraction`
   - 保存

### 5.3 连接死亡 → GameOver (5分钟)

在 `BP_PlayerCharacter` → `AC_HealthComponent` → `OnDeath` 事件：
```
AC_HealthComponent → Event OnDeath:
  → GetGameMode → Cast to GM_Extraction
  → Call "PlayerDied"
```

### 5.4 验证

1. Play → 跑到撤离点 → 看到进度提示（临时用 Print String）
2. 倒计时结束 → 提示胜利
3. 被敌人打死 → 提示失败
4. 时间耗尽 → 提示失败

---

## Day 6: UI/HUD（血量、弹药、背包、结果画面）

**目标：** 屏幕上有完整的玩家状态信息

### 6.1 创建 HUD (45分钟)

1. 内容浏览器(Content Browser) 右键 → 用户界面(User Interface) → **控件蓝图(Widget Blueprint)** → 命名 `WBP_HUD`
2. 设计布局（拖控件到 画布面板(Canvas Panel)）：

```
画布面板(Canvas Panel) (根 Root)
├── 横向框(Horizontal Box) [左上角] — 血量区域
│   ├── 文本块(TextBlock) "HP:"
│   └── 进度条(ProgressBar)（绑定血量百分比）— 命名 PB_Health
│
├── 纵向框(Vertical Box) [右上角] — 武器/弹药
│   ├── 文本块(TextBlock) "武器: " + 文本块（绑定武器名）— 命名 TXT_WeaponName
│   └── 文本块(TextBlock) "弹药: " + 文本块（绑定弹药）— 命名 TXT_Ammo
│
├── 文本块(TextBlock) [底部居中] — 交互提示
│   └── "按 E 拾取"（默认隐藏，可交互时显示）— 命名 TXT_InteractPrompt
│
├── 文本块(TextBlock) [顶部居中] — 倒计时
│   └── "剩余时间: 30:00" — 命名 TXT_Timer
│
└── 文本块(TextBlock) [底部居中] — 撤离进度
    └── "撤离中... 3s"（默认隐藏）— 命名 TXT_ExtractionProgress
```

3. **数据绑定（绑定蓝图变量/函数）：**

在 WBP_HUD 的事件图表(Event Graph)：
```
事件 构建(Construct):
  → 获取所属玩家Pawn(Get Owning Player Pawn) → 转换为(Cast To) BP_ThirdPersonCharacter
  → 存为 "PlayerRef" 变量
  → 绑定事件: PlayerRef → AC_HealthComponent → OnHealthChanged
    → 更新血量条
  → 绑定事件: PlayerRef → EquippedWeapon → OnAmmoChanged
    → 更新弹药文本

事件 Tick:
  → 更新 TXT_Timer = 格式化文本(Format Text)(GetGameMode → Get RemainingTime)
  → 更新 TXT_InteractPrompt (根据 CanInteract)
  → 更新 TXT_ExtractionProgress (根据是否在撤离区域)
```

### 6.2 创建背包 UI (30分钟)

1. 内容浏览器(Content Browser) 右键 → 控件蓝图(Widget Blueprint) → 命名 `WBP_Inventory`
2. 设计布局：

```
画布面板(Canvas Panel)
├── 边框(Border) [背景] — 半透明黑底
├── 文本块(TextBlock) "背包" [顶部居中]
├── 包裹框(Wrap Box) [网格排列物品格]
│   └── 每个格: WBP_InventorySlot（子控件(Widget)）
│       ├── 图像(Image)（物品图标）
│       ├── 文本块(TextBlock)（物品名）
│       └── 文本块(TextBlock)（数量）
└── 按钮(Button) "关闭" [右上角]
```

3. 打开背包逻辑（Enhanced Input 方式）：
   - 创建 输入动作(Input Action) `IA_Inventory` → 值类型选 数字(布尔)(Digital (bool))
   - 打开 `IMC_Default` → Default Key Mappings（默认按键映射） → `+` → 选 `IA_Inventory` → 按键选 **Tab**
   - 在 `BP_ThirdPersonCharacter` 事件图表添加：
```
增强型输入动作(EnhancedInputAction) (IA_Inventory) → 已开始(Started)
  → 创建控件(Create Widget) (WBP_Inventory)
  → 添加到视口(Add to Viewport)
  → 设置输入模式游戏和UI(Set Input Mode Game and UI)
  → 显示鼠标光标(Show Mouse Cursor)

关闭逻辑 → 从父项移除(Remove from Parent)
  → 设置输入模式仅游戏(Set Input Mode Game Only)
  → 隐藏鼠标光标(Hide Mouse Cursor)
```

### 6.3 创建结果画面 (15分钟)

1. 控件蓝图(Widget Blueprint) → 命名 `WBP_GameResult`
2. 布局：
```
画布面板(Canvas Panel)
├── 边框(Border) [半透明黑背景]
├── 文本块(TextBlock) [居中] — 绑定结果文字（撤离成功/死亡/时间耗尽）
│   命名 TXT_ResultTitle
├── 文本块(TextBlock) [居中偏下] — 显示统计数据
│   命名 TXT_Stats
├── 按钮(Button) "重新开始"
│   点击时(OnClicked) → 打开关卡(OpenLevel) (L_Extraction)
└── 按钮(Button) "退出"
    点击时(OnClicked) → 退出游戏(Quit Game)
```

### 6.4 给 Player 创建 HUD (5分钟)

在 `BP_ThirdPersonCharacter` → 事件 BeginPlay:
```
事件 BeginPlay:
  → 创建控件(Create Widget) (WBP_HUD)
  → 添加到视口(Add to Viewport)
  → 存为 "HUDRef" 变量
```

### 6.5 验证

1. Play → 左上角血量条可见
2. 捡起武器 → 右上角弹药显示
3. 走近物品 → 底部出现 "按 E 拾取"
4. 按 Tab → 背包打开/关闭
5. 撤离/死亡 → 结果画面出现

---

## Day 7: 打磨 + 音效 + 打包测试

**目标：** 修复明显 bug，加基础音效，做一个可运行的包

### 7.1 Bug 修复清单 (60分钟)

逐项测试：
- [ ] 捡起多个物品，背包满了怎么办？（加判断 `Inventory.Num() < MaxSlots`）
- [ ] 武器没子弹了按射击 → 要有反馈（干火音效或提示）
- [ ] 敌人死后还会不会追？（OnDeath → Set AIState = Dead → 停止 Tick 逻辑）
- [ ] 撤离时被打断（走出区域）→ 倒计时是否重置？
- [ ] 切换关卡后会不会有残留 UI？（Event EndPlay → Remove All Widgets）
- [ ] 敌人看到玩家但有障碍物 → 射线检测是否生效？

### 7.2 基础音效 (30分钟)

1. 从免费音效网站（freesound.org / zapsplat.com）下载：
   - 枪声（手枪、步枪）
   - 拾取音效
   - 脚步声（暂时跳过，太复杂）
   - 撤离成功音效
   - 受伤/死亡音效
2. 导入：内容浏览器(Content Browser) → 拖 WAV/MP3 文件进来
3. 在对应蓝图里添加：
   - `BP_WeaponBase::Fire` → **在位置播放声音(Play Sound at Location)**（枪口位置）
   - `BP_PickupBase::拾取成功时` → **播放声音2D(Play Sound 2D)**
   - `AC_HealthComponent::TakeDamage` → **播放声音2D(Play Sound 2D)**（受伤叫声）
   - `GM_Extraction::PlayerExtracted` → **播放声音2D(Play Sound 2D)**（胜利音）

### 7.3 简单特效 (30分钟)

1. Niagara 粒子系统：
   - 右键 → FX → **Niagara System** → 用模板 `Simple Sprite Burst`
   - 枪口闪光：放在 `BP_WeaponBase::Fire` 里 Spawn
   - 命中特效：LineTrace Hit Location → Spawn Niagara

### 7.4 打包测试 (30分钟)

1. 菜单栏 → Platforms → Windows → **Package Project**
2. 选输出目录 → 开始打包
3. 打包完成后运行 exe 测试：
   - 检查独立运行时是否正常
   - 检查帧率
   - 检查 UI 是否适配屏幕

### 7.5 如果还有时间 (30分钟)

- 给敌人加简单的死亡动画（Ragdoll: SetAllBodiesSimulatePhysics）
- 添加小地图（SceneCaptureComponent2D + RenderTarget）
- 添加更多武器类型
- 添加多个敌人 + 不同巡逻路线

---

## 每日时间分配

| 天 | 重点 | 预计时间 |
|----|------|---------|
| Day 1 | 项目创建 + 地形 + 角色移动 | 1.5h |
| Day 2 | 拾取系统 + 背包 + 宝箱搜索 | 3h |
| Day 3 | 武器系统（射击+伤害） | 2.5h |
| Day 4 | AI 敌人 | 2.5h |
| Day 5 | 撤离点 + GameMode | 2h |
| Day 6 | HUD/UI 全套 + 战利品面板 | 2.5h |
| Day 7 | 打磨 + 音效 + 打包 | 2h |
| **总计** | | **~16h** |

---

## 免费素材来源

| 类型 | 来源 |
|------|------|
| 角色模型/动画 | Mixamo.com（Adobe 免费） |
| 武器模型 | Epic Marketplace → 搜 "Free Weapon" |
| 环境资产 | Epic Marketplace → "Free" 筛选 → "Modular Neighborhood Pack" |
| 音效 | freesound.org / zapsplat.com |
| 图标 | game-icons.net（CC 许可） |
| UI 素材 | 自己用 Figma 画简单方框 / 用 UE 自带 |

---

## 前置工作清单（现在就可以做）

1. [ ] 启动 Epic Launcher → 安装 UE 5.4 或 5.5
2. [ ] 在 Epic Marketplace 领取免费资产（搜 "Free" + "Permanent Collection"）
3. [ ] 注册 Mixamo 账号
4. [ ] 下载 Visual Studio 2022（如果没装，勾选 "Game Development with C++" workload）
5. [ ] 在 UE Marketplace 下载 "Content Examples" 项目（学习参考用）

---

## 遇到问题时的排查顺序

1. **蓝图编译报错** → 检查变量类型是否匹配，Cast 是否成功
2. **角色不能动** → 检查 PlayerController 和 GameMode 是否正确指定
3. **碰撞不触发** → 检查 Collision Preset 是否设置 Overlap
4. **AI 不动** → 检查 NavMeshBoundsVolume 是否覆盖了地图
5. **UI 不显示** → 检查 AddToViewport 是否被调用，ZOrder 是否被遮挡

---

## 完整体验流程（塔科夫风格）

### 一场典型的战局

```
你出生在地图边缘 → 手里没武器
    ↓
跑向最近的建筑物/掩体区域
    ↓ 发现地面拾取物
按 E 捡起一把手枪 → 自动装备到手上
    ↓ 发现一个木箱
按住 F 搜索 → 进度条走 2 秒 → 打开战利品面板
    → 箱子里有: 5.56弹药×45、绷带×2、金链×1
    → 点击物品拖入背包（12格背包管理）
    ↓ 搜索第二个箱子（行李袋）
    → 里面只有胶带×2
    ↓ 听到脚步声，AI 巡逻敌人靠近
发现敌人 → AI 转为追击 → 你开火
    → 击毙敌人，敌人尸体旁什么都没有（简化处理）
    ↓ 血量只剩 30
使用背包里的绷带 → 血量 +25
    ↓ 时间已过 20 分钟，倒计时显示 10:00
决定撤离
    → 跑到撤离点 A → 提示"需要特殊钥匙"
    → 继续找撤离点 B → 免费撤离点
    → 走进绿光区域 → "撤离中... 5, 4, 3, 2, 1"
    ↓
画面变黑 → "撤离成功！"
    → 显示本次战利品清单: 金链、5.56弹药、胶带...
    → 按钮: [重新开始] [退出]
```

### 与塔科夫的对应

| 塔科夫 | 本项目 |
|--------|--------|
| SCAV 巡逻 | BP_EnemyCharacter Tick AI 巡逻+追击+攻击 |
| 搜刮容器 | 按住 F 搜索木箱/行李袋/武器箱 |
| 背包管理 | 12格背包，战利品面板拖拽转移 |
| 贵重品 | Valuables 分类（金链/比特币等） |
| 多撤离点 | 2~3 个撤离点，部分有条件限制 |
| 30分钟限时 | 倒计时 1800 秒 |
| 死亡丢装备 | 死亡 → GameOver 失败，战利品丢失 |
| 保险/藏身处 | 不做（需求太大） |
| 商人/跳蚤市场 | 不做（单机没必要） |

---

## 验证方式

每天结束时按「验证」步骤执行 Play 测试，确认当天目标功能的行为是否正确。Day 7 打包后在独立 exe 中跑一遍完整游戏循环。

---

## UE5 关键节点中英文对照表

### 蓝图节点（事件图表搜索用）

| 中文名 | 英文名（搜索用） |
|--------|---------------|
| 分支 | Branch |
| 转换为 | Cast To |
| 设置 | Set |
| 获取 | Get |
| 构造 | Make |
| 拆分 | Break |
| 销毁Actor | DestroyActor |
| 生成Actor | SpawnActor |
| 延迟 | Delay |
| 数组 ADD | Array Add |
| 数组 长度 | Array Length |
| 序列 | Sequence |
| DoOnce | Do Once |
| 翻转 | Flip Flop |
| 门 | Gate |
| 时间轴 | Timeline |
| 广播 | Broadcast / Event Dispatcher |
| 打印字符串 | Print String |
| 格式化文本 | Format Text |
| 增强型输入动作 | EnhancedInputAction |
| 已开始 | Started |
| 已完成 | Completed |
| 触发中 | Triggered |
| 组件开始重叠时 | OnComponentBeginOverlap |
| 组件结束重叠时 | OnComponentEndOverlap |
| 获取类的所有Actor | GetAllActorsOfClass |
| 获取距离到 | GetDistanceTo |
| 按通道射线追踪 | LineTraceByChannel |
| AI 移动到 | AI MoveTo |
| 获取Actor前向矢量 | GetActorForwardVector |
| 获取世界位置 | GetWorldLocation |
| 设置Actor旋转 | SetActorRotation |
| 寻找看向的旋转 | FindLookAtRotation |
| 将Actor附加到组件 | AttachActorToComponent |
| 在位置生成发射器 | SpawnEmitterAtLocation |
| 在位置播放声音 | Play Sound at Location |
| 播放声音2D | Play Sound 2D |
| 创建控件 | Create Widget |
| 添加到视口 | Add to Viewport |
| 从父项移除 | Remove from Parent |
| 设置输入模式仅游戏 | Set Input Mode Game Only |
| 设置输入模式游戏和UI | Set Input Mode Game and UI |
| 显示/隐藏鼠标光标 | Show/Hide Mouse Cursor |
| 打开关卡 | OpenLevel |
| 退出游戏 | Quit Game |

### 资产类型（内容浏览器创建用）

| 中文名 | 英文名 |
|--------|--------|
| 蓝图类 | Blueprint Class |
| 控件蓝图 | Widget Blueprint |
| 输入动作 | Input Action |
| 输入映射上下文 | Input Mapping Context |
| 枚举 | Enumeration |
| 数据表 | Data Table |
| 关卡 | Level |
| Niagara 系统 | Niagara System |
| 声音提示 | Sound Cue |

### 组件类型（+添加面板）

| 中文名 | 英文名 |
|--------|--------|
| 静态网格体 | Static Mesh |
| 骨骼网格体 | Skeletal Mesh |
| 球体碰撞 | Sphere Collision |
| 盒体碰撞 | Box Collision |
| 箭头 | Arrow |
| Actor组件 | Actor Component |
| 定向光源 | Directional Light |
| 天空光照 | Sky Light |
| 天空大气 | Sky Atmosphere |
| 体积云 | Volumetric Cloud |
| 玩家出生点 | Player Start |
| 导航网格边界体积 | NavMeshBoundsVolume |

### UI 控件（Widget 蓝图内）

| 中文名 | 英文名 |
|--------|--------|
| 画布面板 | Canvas Panel |
| 横向框 | Horizontal Box |
| 纵向框 | Vertical Box |
| 包裹框 | Wrap Box |
| 边框 | Border |
| 文本块 | TextBlock |
| 进度条 | ProgressBar |
| 图像 | Image |
| 按钮 | Button |

### 常用菜单/面板

| 中文名 | 英文名 |
|--------|--------|
| 内容浏览器 | Content Browser |
| 细节/细节面板 | Details Panel |
| 我的蓝图 | My Blueprint |
| 事件图表 | Event Graph |
| 世界设置 | World Settings |
| 项目设置 | Project Settings |
| Play / 运行 | Play / Play In Editor (PIE) |
| 编译 | Compile |
| 保存 | Save |
| 地形 | Landscape |
| 雕刻 | Sculpt |
